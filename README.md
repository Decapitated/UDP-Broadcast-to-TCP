# UDP Broadcast to TCP Connection
A basic implementation of a server/client flow to exchange LAN info without prior knowledge.
(Except) UDP port & Server Secret

## Requirements
- Latest Python (3.10.5)